# Digital Twin — CLI Commands

```
hoa simulate <plan_id>              → run plan against knowledge graph
hoa simulate --plan-file <path>     → simulate from file
hoa simulate --dry-run              → show what would happen without running
hoa conflict list                   → show all unresolved conflicts
hoa conflict resolve <id>           → manually resolve a conflict
```